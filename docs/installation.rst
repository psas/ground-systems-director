============
Installation
============

At the command line::

    $ easy_install psas_services

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv psas_services
    $ pip install psas_services
